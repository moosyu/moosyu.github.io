{% extends "base.njk" %}

{% set guestbook_css %} {% include "../../css/jb-comments.css" %} {% endset %}

{% block head %}
    <style>
        {{ guestbook_css | cssmin | safe }}
        .media {
            width: 300px;
            flex-shrink: 0;
        }
        .media img {
            border: 2px white dotted;
            border-radius: 20px;
        }
        .media img:hover {
            cursor: pointer;
        }
        .thoughts-content {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        @media screen and (min-width: 600px) {
            .thoughts-content {
                flex-direction: row;
            }
        }
    </style>
    <noscript>
        <style>
            .comment-section {
                display: none;
            }
        </style>
    </noscript>
{% endblock %}

{% block content %}

    <div class="background-div">
        <div style="display: flex; gap: 10px;" class="thoughts-content">
            {{ content | safe }}
        </div>
        <div class="comment-section">
            <hr>
            <h2>What'd you think?</h2>
            <div id="comment-entry-form"></div>
            <div id="comment-section">
                <span class="comment-loading">Comments loading...</span>
            </div>
        </div>
    </div>

    <script>
    function toggleFullscreen(img) {
        if (!document.fullscreenElement) {
            img.requestFullscreen();
        } else {
            document.exitFullscreen();
        }
    }
    </script>
    <script src="/js/jb-comments.js"></script>
{% endblock %}